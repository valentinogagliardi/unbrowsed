Installation
============

Requirements
------------

unbrowsed requires Python 3.8 or higher.

Basic Installation
------------------

You can install Unbrowsed using pip:

.. code-block:: bash

    pip install unbrowsed
